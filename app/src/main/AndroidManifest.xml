<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
        specialUse: only FGS type without the 6h/24h `dataSync` cap on
        Android 14+. Justified for sideloaded distribution by the
        <property> on each <service>; Play Store would require review of
        that property value, which doesn't apply here.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
        Watchdog alarm restarts the FGS if it gets killed (OEM battery
        managers, low memory, transient network issues). USE_EXACT_ALARM is
        granted at install time on Android 13+; SCHEDULE_EXACT_ALARM is the
        runtime-grantable variant for Android 12 and the user-revocable
        equivalent on 14+.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!--
        Lets us send the user to the per-app battery optimization screen so
        they can grant the exemption that keeps the FGS alive long-term.
    -->
    <uses-permission
        android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".KumaCheckApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.KumaCheck">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.KumaCheck">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notify.MonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="continuous_uptime_kuma_socket_monitoring" />
        </service>

        <service
            android:name=".notify.NtfyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="continuous_ntfy_push_subscription" />
        </service>

        <receiver
            android:name=".notify.WatchdogReceiver"
            android:exported="false" />

        <!--
            Unexported: BOOT_COMPLETED and MY_PACKAGE_REPLACED are protected
            broadcasts that the system delivers to unexported receivers in the
            target package (with RECEIVE_BOOT_COMPLETED for the boot signal,
            held above). External apps can't directly target this receiver,
            which avoids the explicit-component-intent attack vector.
        -->
        <receiver
            android:name=".notify.BootReceiver"
            android:exported="false"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notify.widget.StatusTileWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_status_tile_info" />
        </receiver>

        <receiver
            android:name=".notify.widget.StatusListWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_status_list_info" />
        </receiver>

        <receiver
            android:name=".notify.widget.StatusMicroWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_status_micro_info" />
        </receiver>
    </application>
</manifest>
